on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - id: foo
      uses: ${{ github.repository }}/hello
      with:
        who-to-greet: "Jamie"

    - run: echo random-number ${{ steps.foo.outputs.random-number }} 
      shell: bash
